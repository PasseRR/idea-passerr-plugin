package ${serviceImpl.packageName};

import com.baomidou.mybatisplus.extension.service.impl.ServiceImpl;
import org.springframework.stereotype.Service;
import ${entity.fullClassName};
import ${mapper.fullClassName};

/**
 * ${entity.desc}服务
 * @author $author
 * @date $date
 */
@Service
public class ${service.className} extends ServiceImpl<${mapper.className}, ${entity.className}> {
}
